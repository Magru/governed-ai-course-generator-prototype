artifact: guards
question: "Which layer owns each guard, and what does it hand back when it refuses?"
owner: "system architect"
updated_when: "a guard is added, renamed, or changes owner"
source: "transitions.html §4 — the guard glossary, one entry per name"
generated: "2026-09-02"
generator: "model/export-model.py"

# The transition tables' Layer column lists everything that runs at a
# transition. This file answers the different question of who owns a
# particular guard — the two were briefly mistaken for each other.

count: 38
guards:
  - name: "schema_valid(artifact)"
    owner: "JSON Schema"
    refuses_with: "the failing path and the expected type"
  - name: "policy_allows(action, role, state)"
    owner: "OPA"
    refuses_with: "the rule that denied and a human-readable reason. One of two guards whose refusal is terminal — the other is guardrail_clean on the brief, where a denied topic ends the revision rather than sending it"
  - name: "guardrail_clean(artifact)"
    owner: "Managed guardrail"
    refuses_with: "a category, never with a matched string. An explicit deny is a refusal; an unreachable service is a timeout, which is an unknown and routes to recovery — ErrorRecovery when the artifact is a brief, No"
  - name: "feasible(brief)"
    owner: "Z3"
    refuses_with: "an unsat core: the smallest set of requirements that cannot hold together"
  - name: "skills_grounded(outline)"
    owner: "Datalog"
    refuses_with: "naming the invented skills"
  - name: "grounded(node)"
    owner: "Datalog"
    refuses_with: "naming the unsupported claims"
  - name: "references_live(node)"
    owner: "Datalog"
    refuses_with: "naming the reference and the outline version that dropped its target"
  - name: "no_permission_leak(scope)"
    owner: "Datalog"
    refuses_with: "the path: node → source → the audience that lacks rights"
  - name: "ordering_acyclic(course)"
    owner: "Datalog"
    refuses_with: "the cycle"
  - name: "objectives_covered(scope)"
    owner: "Prolog"
    refuses_with: "the uncovered objectives, which is also what drives the editor's next-best-action"
  - name: "content_approved(topics(exam))"
    owner: "Datalog"
    refuses_with: "naming the topics still outstanding"
  - name: "depends_on(node, edited)"
    owner: "Datalog"
  - name: "trace_satisfies_ltl(course)"
    owner: "Temporal logic"
    refuses_with: "the violated formula and the event that broke it"
  - name: "block_schemas_valid(node)"
    owner: "Catalog schema"
  - name: "hand_edited(node)"
    owner: "State store"
  - name: "all_visuals_reviewed(node)"
    owner: "Catalog flag · Human"
    refuses_with: "the queue of unreviewed visuals"
  - name: "course_state(revision)"
    owner: "State store"
  - name: "outline_version(revision)"
    owner: "State store"
  - name: "has_nodes(revision)"
    owner: "State store"
  - name: "has_outline(revision)"
    owner: "State store"
  - name: "reason_given(action)"
    owner: "State store"
  - name: "arithmetic_consistent(node)"
    owner: "Z3"
    refuses_with: "the failing sum"
  - name: "notice_approved(recipient_set)"
    owner: "Human"
  - name: "lost_live_pointer(revision)"
    owner: "State store"
  - name: "blocked_at(revision)"
    owner: "State store"
  - name: "in_outline(node, committed_outline)"
    owner: "State store"
    refuses_with: "the outline version that dropped it"
  - name: "all_nodes_approved(revision)"
    owner: "State store"
    refuses_with: "the offending nodes"
  - name: "no_stale_nodes(revision)"
    owner: "State store"
    refuses_with: "the offending nodes"
  - name: "recovery_from(node)"
    owner: "State store"
  - name: "retry_budget_left(scope)"
    owner: "State store"
  - name: "idempotency_key_unused(operation)"
    owner: "State store"
  - name: "versions_current(artifact)"
    owner: "State store"
  - name: "in_live_lineage(revision)"
    owner: "State store"
  - name: "is_draft_revision(revision)"
    owner: "State store"
  - name: "differs_from_live(revision)"
    owner: "State store"
  - name: "affected(revision)"
    owner: "Datalog · Guardrail · State store"
  - name: "has_active_readers(revision)"
    owner: "Datalog · Guardrail · State store"
  - name: "approval_chain_satisfied(revision)"
    owner: "OPA"
